{
  "name": "chartml-spec-tests",
  "version": "1.0.0",
  "description": "Tests to validate ChartML specification examples against the JSON schema",
  "type": "commonjs",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:verbose": "jest --verbose"
  },
  "devDependencies": {
    "ajv": "^8.12.0",
    "jest": "^29.7.0",
    "yaml": "^2.3.4"
  }
}
