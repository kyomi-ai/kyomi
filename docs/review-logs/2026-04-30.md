# Code Review Log — 2026-04-30

---

### 09:00 — Task 14: SQL editor migration to fetch_arrow_buffered (re-review cycle 2)

**Review type**: re-review (cycle 2)
**Result**: 1 🔴, 0 🟡, 0 🟢
**Files**: `crates/kyomi-ui/src/pages/sql_editor/execution.rs`, `crates/kyomi-ui/src/pages/sql_editor/results_container.rs`, `crates/kyomi-ui/src/pages/sql_editor/results_table.rs`, `crates/kyomi-ui/src/pages/sql_editor/state.rs`, `crates/kyomi-ui/src/pages/sql_editor/types.rs`, `crates/kyomi-ui/src/server_fns/sql_editor.rs`

Issues from cycle 1: 3/3 major resolved, 3/3 minor resolved. One new critical introduced during the fix.

| # | Severity | Anti-Pattern Category | File:Line | Description |
|---|----------|----------------------|-----------|-------------|
| 1 | 🔴 | Quick & Dirty | execution.rs:118 | `datasource_type` used inside `#[cfg(target_arch="wasm32")]` closure but is not a parameter of `run_arrow_query` — compiles on native (cfg skips the body), fails on WASM target with E0425 |

**Anti-pattern tally**: Quick & Dirty ×1 (parameter threading incomplete)

**Notes**: The fix for the `datasource_type` threading from the previous cycle correctly threaded the value through `run_query` → `NewTabData` → stored on the tab, and also correctly reads it from `handle.datasource_type` in `results_container.rs`. The gap is that `run_arrow_query` doesn't accept `datasource_type` as a parameter — the value is available in `run_query`'s scope but is not forwarded. The WASM-only `#[cfg]` guard meant `cargo check` on the native target silently passed. Always verify with `--target wasm32-unknown-unknown`.
